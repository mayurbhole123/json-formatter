apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: json-formatter

resources:
  - namespace.yaml
  - configmap.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - hpa.yaml

images:
  - name: json-formatter
    newTag: 1.0.0
